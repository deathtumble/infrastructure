#!/bin/bash

set -e
directory=`dirname $0`

environment=${PWD##*/} 

terraform output -json > output.json1

address=$(jq ".aws_alb_default_dns_name.value" output.json -r ) 

echo $address
dig $address