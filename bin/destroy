#!/bin/bash

set -e
directory=`dirname $0`
source terraform.tfvars

if terraform destroy -force ;  then

    $directory/support/slack terraform destroy to ${product}-${environment} has succeed
else 
    $directory/support/slack terraform destroy to ${product}-${environment} has failed
fi    