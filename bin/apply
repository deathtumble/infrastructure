#!/bin/bash

set -e
directory=`dirname $0`
source config.vartf

if  $directory/support/terraform apply -auto-approve ; then

    $directory/support/slack terraform apply to ${product}-${environment} has succeed
else
    $directory/support/slack terraform apply to ${product}-${environment} has failed
fi    

