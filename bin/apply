#!/bin/bash

set -e
directory=`dirname $0`
source $directory/variablefile

if  terraform apply -auto-approve -var-file=${variablefiles} ; then

    $directory/configConsul

    $directory/slack terraform apply to ${product}-${environment} has succeed
else
    $directory/slack terraform apply to ${product}-${environment} has failed
fi    

