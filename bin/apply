#!/bin/bash

set -e
directory=`dirname $0`
source setenv

if  terraform apply -auto-approve -var-file=config.vartf ; then

    $directory/configConsul ${product} ${environment}

    $directory/slack terraform apply to ${product}-${environment} has succeed
else
    $directory/slack terraform apply to ${product}-${environment} has failed
fi    

