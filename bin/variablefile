#!/bin/bash

set -e

environment=$1
if [ -z "$environment" ]; then
    environment=poc
fi    

product=$2
if [ -z "$product" ];  then
    product=poc
fi 

variablefiles=${product}_${environment}_config.vartf     

